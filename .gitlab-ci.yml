image: node:latest

variables:
  PUBLIC_URL: /buymecoffee

cache:
  paths:
  - node_modules

stages:
  - deploy

pages:
  stage: deploy

  script:
    - npm install
    - npm run build
    
  artifacts:
    paths:
    - public

  only:
    - main
